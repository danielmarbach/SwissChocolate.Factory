@{
    ViewBag.Title = "Home Page";
}

<div class="jumbotron">
    <h1>Chocolate Facility</h1>
</div>

<div class="row">
    <div class="col-xs-12">
        @using (Html.BeginForm("Index", "Produce", FormMethod.Post))
        {
        <button type="submit" class="btn btn-default btn-lg">
            <span class="glyphicon glyphicon-star" aria-hidden="true"></span> Produce chocolate
        </button>
        }

    </div>
</div>